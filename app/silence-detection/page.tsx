import ProblemPage, { problemMetadata } from "@/components/seo/problem-page";

export const metadata = problemMetadata("silence-detection");

export default function Page() {
  return <ProblemPage slug="silence-detection" />;
}
