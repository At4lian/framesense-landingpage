import ProblemPage, { problemMetadata } from "@/components/seo/problem-page";

export const metadata = problemMetadata("black-frame-detection");

export default function Page() {
  return <ProblemPage slug="black-frame-detection" />;
}
