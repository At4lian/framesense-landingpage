import ProblemPage, { problemMetadata } from "@/components/seo/problem-page";

export const metadata = problemMetadata("loudness-checker-lufs");

export default function Page() {
  return <ProblemPage slug="loudness-checker-lufs" />;
}
