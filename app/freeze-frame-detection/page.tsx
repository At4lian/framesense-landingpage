import ProblemPage, { problemMetadata } from "@/components/seo/problem-page";

export const metadata = problemMetadata("freeze-frame-detection");

export default function Page() {
  return <ProblemPage slug="freeze-frame-detection" />;
}
