import UseCasePage, { useCaseMetadata } from "@/components/seo/use-case-page";

export const metadata = useCaseMetadata("video-production");

export default function Page() {
  return <UseCasePage slug="video-production" />;
}
