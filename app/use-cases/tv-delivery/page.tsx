import UseCasePage, { useCaseMetadata } from "@/components/seo/use-case-page";

export const metadata = useCaseMetadata("tv-delivery");

export default function Page() {
  return <UseCasePage slug="tv-delivery" />;
}
